name: CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: status-im/nimbus-common-workflow/.github/workflows/common.yml@83d59091055c598839962569aa727c2be790fb68
    with:
      test-command: |
        env NIMLANG=c nimble test
        env NIMLANG=cpp nimble test
